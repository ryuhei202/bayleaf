import { TCoordinateResponse } from "../../api/coordinates/TCoordinateResponse";

type TProps = {
  coordinate: TCoordinateResponse;
};

export const DesignConsultContainer = ({ coordinate }: TProps) => {
  return <div>DesignConsultContainer</div>;
};
