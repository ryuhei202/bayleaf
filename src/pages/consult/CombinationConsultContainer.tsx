import { TCoordinateResponse } from "../../api/coordinates/TCoordinateResponse";

type TProps = {
  coordinate: TCoordinateResponse;
};

export const CombinationConsultContainer = ({ coordinate }: TProps) => {
  return <div>CombinationConsultContainer</div>;
};
