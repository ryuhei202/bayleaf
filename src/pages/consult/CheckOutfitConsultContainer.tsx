import { TCoordinateResponse } from "../../api/coordinates/TCoordinateResponse";

type TProps = {
  coordinate: TCoordinateResponse;
};

export const CheckOutfitConsultContainer = ({ coordinate }: TProps) => {
  return <div>CheckOutfitConsultContainer</div>;
};
