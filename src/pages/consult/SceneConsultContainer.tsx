import { TCoordinateResponse } from "../../api/coordinates/TCoordinateResponse";

type TProps = {
  coordinate: TCoordinateResponse;
};

export const SceneConsultContainer = ({ coordinate }: TProps) => {
  return <div>SceneConsultContainer</div>;
};
